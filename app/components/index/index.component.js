import template from './index.html';
import controller from './index.controller';

let indexComponent = {
    restrict: 'AE',
    bindings: {},
    template,
    controller,
    controllerAs: 'index'
};

export default indexComponent;